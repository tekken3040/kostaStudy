<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kosta.kosta12thread">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AsynTaskActivity"></activity>
        <activity android:name=".CountdownActivity" />
        <activity android:name=".TimeoutActivity" />
        <activity android:name=".ProgressPostActivity" />
        <activity android:name=".ProgressActivity" />
        <activity android:name=".LooperActivity" />
        <activity android:name=".BasicThread2Activity" />
        <activity android:name=".BasicThread1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>